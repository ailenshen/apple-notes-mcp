{
  "name": "@ailenshen/apple-notes-mcp",
  "version": "1.3.0",
  "description": "Apple Notes MCP Server",
  "repository": {
    "type": "git",
    "url": "https://github.com/ailenshen/apple-notes-mcp"
  },
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "apple-notes-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "tsc && node --test --test-isolation=none --test-reporter=spec --test-reporter-destination=stderr dist/*.test.js && node dist/test.js",
    "test:unit": "tsc && node --test --test-isolation=none --test-reporter=spec --test-reporter-destination=stderr dist/*.test.js",
    "test:e2e": "tsc && node dist/test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "express": "^5.2.1",
    "turndown": "^7.2.2"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^24.0.0",
    "@types/turndown": "^5.0.6",
    "typescript": "^5.8.3"
  }
}
